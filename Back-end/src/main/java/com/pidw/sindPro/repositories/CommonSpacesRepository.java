package com.pidw.sindPro.repositories;

import com.pidw.sindPro.domains.spaces.CommonSpaces;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CommonSpacesRepository extends JpaRepository<CommonSpaces, Long> {
}
